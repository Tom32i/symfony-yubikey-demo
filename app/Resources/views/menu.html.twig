<div class="menu">
    <a class="button button-outline button-small" href="{{ path('homepage') }}">Homepage</a>
    <hr>
    <a class="button button-outline button-small" href="{{ path('profile') }}">Profile</a>
    <a class="button button-outline button-small" href="{{ path('profile_fully') }}">Full Profile</a>
    <a class="button button-outline button-small" href="{{ path('profile_two_factor') }}">Two Factor Profile</a>
    <hr>
    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
        <a class="button button-outline button-small" href="{{ path('logout') }}">Logout</a>
    {% else %}
        <a class="button button-outline button-small" href="{{ path('login_simple') }}">Simple Login</a>
        <a class="button button-outline button-small" href="{{ path('login') }}">Two-Factor Login</a>
    {% endif %}
</div>
